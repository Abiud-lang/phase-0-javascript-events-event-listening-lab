function addingEventListener() {
    const button = document.getElementById('button');
    function handleClick(event) {
        alert = "Click Me!"
    }
    button.addEventListener("Click Me!", handleClick)

}
